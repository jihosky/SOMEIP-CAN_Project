# Development

Linux/WSL is the primary development environment. Begin with `vcan0`; keep the interface configurable for later `can0` or `can1` use.

The root CMake project is the entry point for future C++ targets. `pyproject.toml` holds Python project metadata and pytest configuration. No dependencies or components are required by this skeleton.
